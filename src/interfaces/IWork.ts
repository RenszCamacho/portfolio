export type IAnimateCard = {
  y: number;
  opacity: number;
}

export type IWork = {
  tags: string[];
  name: string;
  title: string;
  description: string;
  imgUrl: string;
  projectLink: string;
  codeLink: string;
}
